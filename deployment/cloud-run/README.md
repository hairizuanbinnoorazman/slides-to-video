# Deployment to Cloud Run platform

We would need to set up the following:

- Create of Buckets (To handle data)
- Create of Pubsub queues (To handle async workloads)
- Cloud Run Services (Deployed as microservices)